import { kv } from '@vercel/kv';

/**
 * Vercel KV wrapper.
 *
 * Note: Vercel has deprecated KV in favor of marketplace Redis providers,
 * but existing projects can still use @vercel/kv.
 */
export { kv };
